JSON，指复杂对象转换为字符串。

Js方法：
JSON.stringify(value[, replacer[, space]]) 将对象转换为 JSON。
JSON.parse(text[, reviver]) 将 JSON 转换回对象。

JSON 编码的对象与对象字面量有几个重要的区别：
a.字符串使用双引号。JSON 中没有单引号或反引号。所以 'John' 转成 "John"。
b.对象属性名称也是双引号的。这是强制性的。所以 age:30 转成 "age":30。

注意：对象不得有循环引用。
