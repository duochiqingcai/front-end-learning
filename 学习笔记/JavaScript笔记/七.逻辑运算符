Js中有三个逻辑运算符：||或，&&与，!非

1.||或
参与运算的参数有一个为true，则为true。
获取变量或者表达式的第一个真值,如果没有返回最后一个值。
alert(1||0);//1
短路取值。

2.&&与
参与运算参数全为true，则为true。
获取变量或者表达式的第一个假值,如果没有返回最后一个值。
alert(1&&0);//0

与运算 && 在或操作符 || 之前执行，所以代码 a && b || c && d 完全跟 && 表达式加了括号一样： (a && b) || (c && d)。

3.!非
布尔非运算。