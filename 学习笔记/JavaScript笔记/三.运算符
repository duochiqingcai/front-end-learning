1.Js中的运算符优先级

2.
二元运算符+，字符串连接功能
let s= "He"+"llo";
alert(s);//Hello
alert("1"+2);//12,只要其中一个运算元是字符串，则另一个运算元也被转化位字符串。
alert(1+2+"2");//32，先计算左边数字，再字符串连接

一元运算符+，数字转化功能
对非数字运算元，将其转化为数字；如果运算元是数字那就无效，类似Number(...)。
let x=-18;
alert(+x);//-18,数字无效

alert(+true);//1

3.
赋值运算符 =
求余运算符 %
幂运算符 **
自增/自减 ++/-- //注意在变量前后的不同
位运算符 //把运算元当作32位整数，并在二进制形式上操作。
    按位与 &  console.log(5&13); //0101&1101=0101
    按位或 |  console.log(5 | 13); // 0101 | 1101 = 1101
    按位异或 ^
    按位非 ~
    按位左移 <<
    按位右移 >>
    无符号右移 >>>
  